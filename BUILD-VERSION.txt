PGuru — build 2026-09-13-c  (LATEST)

Everything below is IN this zip. Anything claiming to be newer is not.

  0017_push_notifications.py      push notifications
  0018_notification_preference.py per-person on/off switch
  requests==2.32.3                THE FIX that made push actually send
  iconForPath()                   icons on every page header
  broadcast/{id}/stats            live delivery counts

Older zips from this session are SUPERSEDED. Extracting any of them over your
repo will revert the requests fix and push will silently stop sending:

  pgdesk-push-notifications.zip     (2026-09-13-a)  DELETE IT
  pgdesk-notifications-phase2.zip   (2026-09-13-b)  DELETE IT

NO .apk files are in this zip, on purpose. Yours are newer than anything I
could include, and overwriting them is the same mistake in a different place.
Rebuild with:  npm run build:android  &&  gradlew assembleRelease

Verify what you have, from the repo root:

  Select-String -Path backend\requirements.txt -Pattern "^requests"
    -> must print a line. If it prints nothing, push will NOT send.
